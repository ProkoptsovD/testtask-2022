import Input from "../../common/Input";

const FileLoader = () => {
    return (
        <Input
            type="file"
            placeholder="Upload your photo"
        />
    )
}

export default FileLoader;